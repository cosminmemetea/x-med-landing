/** @type {import('next').NextConfig} */
module.exports = {
  // other configuration options...
  output: {
    // Set the output target to export
    export: {
      // Set the path for the exported files (adjust the path according to your needs)
      path: 'out',
    },
  },
};
